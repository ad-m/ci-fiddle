language: none

env:
  matrix:
    - UBUNTU_VERSION: 1404
    - UBUNTU_VERSION: 1604

build:
  ci:
    - wget -h
    - ls -laR
    # - chmod +x cmake/install cmake/versions .ci/install
    - ./cmake/install
    # - for e in "${UBUNTU_VERSIONS[@]}"; do ./docker/build "${CMAKE_VERSION}" $e "sd" "sdf"; done


integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: never
      on_failure: never
      on_cancel: never
      on_start: never
      on_pull_request: never
