language: none

env:
  matrix:
    - VERSION: "1.2.3"
      env:
        - UBUNTU_VERSION='2'
        - UBUNTU_VERSION='5'
    - VERSION: "2.0.0"

build:
  ci:
    - echo "Building CMake ${VERSION} for Ubuntu ${UBUNTU_VERSION}"
