language: none

env:
  matrix:
    - VERSION: "1.2.3"
      UBUNTU_VERSION: 2
    - VERSION: "2.0.0"
      UBUNTU_VERSION: 3

build:
  ci:
    - ls -la
    - echo "Building CMake ${VERSION} for Ubuntu ${UBUNTU_VERSION}"
