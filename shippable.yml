language: none

env:
  matrix:
    - UBUNTU_VERSION: 1404
    - UBUNTU_VERSION: 1604

build:
  ci:
    # - ./cmake/install
    - ./docker/build $UBUNTU_VERSION "sdfdf" "aaaaas"

integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: never
      on_failure: never
      on_cancel: never
      on_start: never
      on_pull_request: never
