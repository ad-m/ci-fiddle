name: Bottle formulae
'on': push
jobs:
  bottle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: |
          echo "Before ${{ github.event.before }}"
          echo "Head ${{ github.event.head }}"
          echo "Size ${{ github.event.size }}"
