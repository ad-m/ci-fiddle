module.exports = {
  scripts: {
    // prerelease: [
    //   'if [[ "${GITHUB_ACTIONS}" != "true" ]] &&',
    //   '[[ "$(git branch --show-current)" != "master" ]];',
    //   'then exit -1; fi',
    // ].join(' '),
  },
};
